<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme -->
    <style name="Theme.Spendlyst" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your app theme here -->
    </style>

    <!-- Splash screen theme -->
    <style name="Theme.Spendlyst.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.Spendlyst</item>
    </style>

</resources>
